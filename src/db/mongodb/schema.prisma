// MongoDB Schema for Reliacare

generator mongodbClient {
  provider = "prisma-client-js"
  output   = "./generated/mongodb-client"
}

datasource mongoDb {
  provider = "mongodb"
  url      = env("MONGODB_DATABASE_URL")
}